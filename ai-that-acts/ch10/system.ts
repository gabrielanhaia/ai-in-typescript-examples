// NOT A LISTING FROM THE BOOK.
//
// `ch10/agent.ts` imports `SYSTEM` from `./system.js`. It is chapter 4's
// string, re-exported here so both loops are pointed at one file — which is
// what makes chapter 14's harness a fair comparison.
export { SYSTEM } from "../ch04/system.js";
